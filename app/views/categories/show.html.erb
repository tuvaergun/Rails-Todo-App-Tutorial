
<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>

<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @category.description %>
</p>

<p>
  <strong>User:</strong>
  <%= @category.user.email %>
</p>

<% @category.items.each do |item| %>
	<li>
		<p><%= item.content %> | <%= link_to 'X', category_item_path(@category, item), method: :delete, data: { confirm: 'Are you sure?' } %></p>
	</li>
<% end %>

<%= link_to 'New İtem', new_category_item_path(@category) %>

